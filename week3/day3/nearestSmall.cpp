#include <iostream>
#include <vector>
#include <math.h>
#include <stack>
#include <utility>
using namespace std;
typedef long long ll;
signed main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    int _t;
    cin >> _t;
    while (_t--) {
        ll n;cin>>n;
        ll arr[n];
        for(int i=0;i<n;i++) cin>>arr[i];
        
    }
}